import React from 'react';

const Collectibles = ({ title = 'Collectibles' }) => {
  return (
    <section className="collectibles">
      <h2>{title}</h2>
      <p>Placeholder content — add your collectibles here.</p>
    </section>
  );
};

export default Collectibles;